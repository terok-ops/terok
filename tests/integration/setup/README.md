# Setup integration tests

Host-side setup workflows exercised through the real `terokctl` CLI:

- `ssh-init`
- config/env path resolution used by setup commands
- other bootstrap flows that do not require Podman containers
